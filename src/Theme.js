import {createTheme} from '@mui/material/styles';

export const theme = createTheme({
  primary: '#0165b1',
  secondary: '#058178',
  palette: {
    primary: {
      main: '#0165b1',
    },
    secondary: {
      main: '#058178',
    },
  },
});

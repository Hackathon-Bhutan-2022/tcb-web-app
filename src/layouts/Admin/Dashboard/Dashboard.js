import React from 'react';
import {Typography} from '@mui/material';

export default function Dashboard() {
  return (
    <React.Fragment>
      <Typography sx={{color: '#d20000'}}>This is Dashboard</Typography>
    </React.Fragment>
  );
}

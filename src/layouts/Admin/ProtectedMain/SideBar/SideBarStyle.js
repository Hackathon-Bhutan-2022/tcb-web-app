import {makeStyles} from '@mui/styles';

export const useStyle = makeStyles(theme => ({
  lagthaLogo: {
    marginTop: '4em',
    marginBottom: '6.688em',
    width: '3em',
  },
  divider: {
    backgroundColor: '#5d5d5d'
  },
}));
